# ⚠️ KRITISCH: DIESE DATEI NIEMALS IN GIT COMMITTEN!
# Add to .gitignore: .env.local
# Diese Datei enthält echte Private Keys und muss lokal geheim bleiben!

# ============================================================================
# TEST WALLETS - NUR FÜR LOKALE TESTS!
# ============================================================================

# Test Wallet 1 (für Transaktions-Tests)
# WICHTIG: 
#   - Nur neue Test-Wallets verwenden!
#   - Nur mit Test-Tokens (Sepolia AERA)!
#   - Private Keys IMMER GEHEIM halten!
#   - Falls Key kompromittiert: SOFORT neue Wallet generieren
TX_TEST_WALLET_1=your_private_key_here_without_0x_prefix

# Test Wallet 2 (für Transaktions-Tests)
TX_TEST_WALLET_2=your_private_key_here_without_0x_prefix

# ============================================================================
# RPC ENDPOINT
# ============================================================================

# Sepolia RPC URL
# Option 1: Public (kann blockiert sein)
SEPOLIA_RPC_URL=https://rpc.sepolia.org

# Option 2: Alchemy (EMPFOHLEN - zuverlässig)
# SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Option 3: Infura
# SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY

# Option 4: BlastAPI (Public)
# SEPOLIA_RPC_URL=https://eth-sepolia.public.blastapi.io

# ============================================================================
# OPTIONAL: WEITERE KONFIGURATION
# ============================================================================

# Etherscan API Key (für Verifikation)
# ETHERSCAN_API_KEY=your_etherscan_key_here

# ============================================================================
# SICHERHEITS-CHECKLISTE
# ============================================================================
# 
# ✅ VOR DEM AUSFÜHREN:
#
# 1. [ ] Diese Datei ist in .gitignore?
#        → Überprüfe: cat .gitignore | grep env.local
#
# 2. [ ] Private Keys sind von NEUEN Test-Wallets?
#        → NICHT von Production/Main-Wallets!
#        → NICHT die Wallets vom Deployment!
#
# 3. [ ] Private Keys haben KEIN 0x Prefix?
#        → Richtig:   abc123def456...
#        → Falsch:    0xabc123def456...
#
# 4. [ ] RPC URL ist funktional?
#        → Teste: curl $SEPOLIA_RPC_URL -H "Content-Type: application/json"
#
# 5. [ ] Test-Wallets haben Test-AERA?
#        → Überprüfe auf Etherscan (Sepolia)
#
# ============================================================================
# HÄUFIGE FEHLER
# ============================================================================
#
# ❌ Error: "undefined" RPC
#    → RPC_URL nicht gesetzt oder falsch formatiert
#
# ❌ Error: "Insufficient balance"
#    → Test-Wallet hat keine AERA Tokens
#
# ❌ Error: "Invalid private key"
#    → Private Key hat 0x Prefix oder falsches Format
#
# ❌ Error: "Invalid seed phrase"
#    → Private Key ist korrupt oder falsch
#
# ============================================================================
